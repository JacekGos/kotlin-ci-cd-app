apiVersion: v1
kind: Service
metadata:
  name: {{ include "kotlin-test-app.fullname" . }}
  labels:
    {{- include "kotlin-test-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kotlin-test-app.selectorLabels" . | nindent 4 }}
